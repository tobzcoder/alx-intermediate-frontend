# 0x00. Semantic HTML

## 📘 Description

This project focuses on understanding and implementing **Semantic HTML** — the use of meaningful HTML elements to enhance both accessibility and SEO.  
Each file in this directory builds upon the previous one, progressively improving structure, metadata, and accessibility.

---

## 🧩 Project Structure

| File             | Description                                                                                                                                             |
| ---------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **0-index.html** | Basic HTML document structure using semantic elements such as `<header>`, `<main>`, `<article>`, `<section>`, and `<footer>`.                           |
| **1-index.html** | Enhanced version of `0-index.html` with meta tags and a proper `<title>` for SEO, responsiveness, and accessibility.                                    |
| **2-index.html** | Full blog post layout created using semantic HTML elements (`<header>`, `<nav>`, `<section>`, `<figure>`, etc.). Demonstrates logical content grouping. |
| **3-index.html** | Adds a contact form with ARIA roles and attributes to improve accessibility for assistive technologies.                                                 |

---

## 🧠 Concepts Covered

- HTML5 document structure
- Semantic elements (`<header>`, `<main>`, `<section>`, `<article>`, `<footer>`, etc.)
- Accessibility best practices (ARIA roles, labels, and attributes)
- SEO optimization with meta tags
- Forms and proper labeling for user inputs

---

## 🚀 How to Use

1. Clone the repository:
   ```bash
   git clone https://github.com/<your-username>/alx-intermediate-frontend.git
   ```
